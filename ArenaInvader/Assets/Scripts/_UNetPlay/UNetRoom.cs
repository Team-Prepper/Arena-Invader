using UnityEngine;

// Reserved for future room/lobby responsibilities if `_UNetPlay`
// splits match settings, player presence, and match start flow out of
// `UNetMatchInfo`. Kept as an explicit stub to avoid confusion with an
// accidentally empty script file.
public class UNetRoom : MonoBehaviour
{
}
